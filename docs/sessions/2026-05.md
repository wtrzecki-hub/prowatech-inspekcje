# Sesje pracy 2026-05 — Prowatech Inspekcje

_Format kontynuujący `docs/sessions/2026-04.md` — najnowsze sesje na górze (chronologia od najnowszej)._

_Patrz [PROGRESS.md](../../PROGRESS.md) dla bieżącego stanu, listy TODO i linków do innych dokumentów._

---

## Pełne wpisy sesji

### 2026-05-08 — Audyt UI 5L cz. 2 (uwagi 1-7) + 6 fix-ów rendererów PDF/DOCX

**Sesja na 2 worktree, 5 PR-ów zmergowanych do main, 4 migracje Supabase, ~3500 linii zmian.** Część implementacyjna trafiła na worktree `claude/charming-neumann-1a82d5` 2026-05-08 (testy + 5 PR-ów na drugim worktree `claude/intelligent-kapitsa-341a76` 2026-05-08). Punktem startowym były 7 nowych uwag Waldka z audytu PDF/DOCX po pełnym dniu z 05-07.

**(1+2+7) Renderery PDF/DOCX pod nowe sekcje + pastele art. 5 PB (PR #13, commit `50707e7`).** Trzy uwagi audytu zbundlowane w jeden PR żeby zachować spójność rendererów (`api/pdf/route.ts` +527 linii, `api/docx/route.ts` +711 linii):
- **pkt 1+2** — drobiazgi rendererów dorzucone do tego samego PR (np. ogólne czyszczenie tabeli VI, dane techniczne wyświetlane z bazy zamiast hardcoded fallbacków).
- **pkt 7** — pastelowe kolory komórek tabeli "Wymagania podstawowe (art. 5 PB)" — zielonkawy (#F0F9F1) dla "spełnia", różowy (#FEF2F2) dla "nie spełnia", szary (#F7F9FB) dla "nie dotyczy". Nowe tokeny `ART5_MET_COLORS_HEX/RGB` w `src/lib/design/protocol-tokens.ts`.

PR #13 zawiera też **6 fix-ów wizualnych** zgłoszonych przez Waldka w trakcie iteracyjnego review wygenerowanego PDF z EW01 (5 generacji v1→v5):
- **A** — szerokości kolumn tabeli VI Zalecenia: zmiana z `[10, 30, 75, 15, 15, 35]` na `[12, 22, 75, 18, 18, 35]` w PDF (i analogicznie 5%/13%/42%/10%/10%/20% w DOCX). Naprawia łamanie nagłówków "Lp" + "." osobno, "Rodza" + "j" osobno, "Pilnoś" + "ć" osobno.
- **B** — `addSubHeading` w jsPDF zawija długie tytuły przez `splitTextToSize(title, pageWidth - 2 * margin)` zamiast pojedynczego `pdf.text(title)`. Globalny fix — naprawia obcięcie tytułu "Metody i środki użytkowania elementów narażonych na szkodliwe wpływy atmosferyczne i niszczące działanie innych czynników" (kończyło się na "i" przy końcu strony, brakowało "innych czynników"). Wcześniejszy ad-hoc fix w PR #11 z 05-07 (split na bold+italic body) jest teraz obsolete, ale zostaje bo nie psuje.
- **C** — kolumna Lp w tabeli II "Sprawdzenie wykonania zaleceń" rozszerzona z 10mm na 12mm. Drobny pattern, identyczny problem łamania jak w fix A.
- **D** — nagłówki sekcji II (osobno dla 5-letniej i rocznej) zawierają nr+datę poprzedniego protokołu. Źródło: `inspections.documents_reviewed.previous_5y/annual.info` (JSONB, wpisywane przez inspektora w metryczce — sekcja "Dokumenty obiektu okazane do wglądu"). Strip prefiks "Okazano, " regexem `^Okazano[\s,—-]*/i` żeby zostawić tylko czysty "nr X/T/YYYY, z dnia DD.MM.YYYY". Dla EW01: "(nr 01/T/2022, z dnia 21.03.2022)" / "(nr 56/T/2025, z dnia 16.04.2025)".
- **E** — pierwsza kontrola 5-letnia (heurystyka `!prev5ySource && five_year.length === 0`): zamiast pustej tabeli z 4 placeholder rzędami renderujemy notkę italic _"Pierwsza kontrola pięcioletnia — brak poprzedniej kontroli tego typu, odniesienie wyłącznie do poprzedniej kontroli rocznej."_. Sekcja roczna renderowana normalnie.
- **F** — pierwsza kontrola roczna (nowa turbina, heurystyka `!prevAnnualSource && annual.length === 0 && all.length === 0`): notka italic _"Pierwsza kontrola roczna obiektu — brak poprzedniej kontroli, brak zaleceń do sprawdzenia."_. Heurystyka bez nowych pól w schemacie — implementuje 3 scenariusze workflow Waldka:
  1. Turbina obsługiwana przez nas (kontynuacja) → `historical_protocols` lub własne `inspections` mają nr+datę
  2. Nowy obiekt → klient udostępnia protokół, inspektor wpisuje do `documents_reviewed`
  3. Pierwsza kontrola → notka, brak tabeli

**(3) Sekcja "Serwis" — domyślnie odznaczona (PR #14, commit `98e16e6`).** Audyt: "Mamy w tej zakładce check do odhaczenia, nie wiem dlaczego przy każdej nowej edycji inspekcji z automatu jest on zaznaczony." Zmiana w `turbine-inspection-form.tsx`: `useState(false)` zamiast `useState(true)` dla `includeServiceInProtocol`. Migracja **`2026-05-08_service_include_default_false.sql`** zmienia DEFAULT kolumny `service_info.include_in_protocol` z TRUE na FALSE — istniejące wiersze zachowują wartości (gotcha: po migracji w bazie EW01 widzimy 2 wiersze service_info dla draftu 73f54344 z TRUE i 8 wierszy dla signed 3eaf4810 z mieszanymi wartościami — duplikaty service_info per inspekcja to istniejący bug danych z wcześniejszych sesji, nie blokuje fix-u). Generatory PDF/DOCX dodatkowo sprawdzają `hasServiceData` — sekcja jest pomijana nawet przy `include_in_protocol=true`, jeśli wszystkie pola puste.

**(4) Zalecenia VI — kolumny urgency/work_kind/element (PR #16, commit `6a20a35`).** Wzór protokołu PIIB wymaga w sekcji VI "Zakres robót remontowych" kolumn "Stopień pilności" i oznaczenia rodzaju robót (K/NB/NG). Dotąd `repair_scope_items` miał tylko opis i termin. Migracja **`2026-05-08_repair_scope_urgency_kind.sql`** dodaje 3 pola:
- `element_name TEXT` — element / lokalizacja roboty (np. "Fundament", "Wieża segment 2")
- `work_kind TEXT CHECK ('K' | 'NB' | 'NG')` — konwencja MSWiA z 16.08.1999 § 3: K=konserwacja, NB=naprawa bieżąca, NG=naprawa główna
- `urgency_level TEXT CHECK ('I' | 'II' | 'III' | 'IV')` — konwencja PIIB/WACETOB: I=natychmiast (zagrożenie), II=do 3 mies., III=do 12 mies., IV=do 5 lat. Konwencja stopni I=najpilniejszy zachowana zgodnie z protokołem 248/T/2025 (mimo że PIIB ma odwrotną — dlatego legenda obowiązkowa w każdym protokole, dodana w PR #13).

Komponent `repair-scope-table.tsx` (+113 linii) rozszerzony o 3 nowe inputy pod istniejącym polem "Zakres czynności": text input z `np. Fundament, Wieża` placeholder + 2 selecty z opcjami (K/NB/NG i I-IV z opisami "K — konserwacja", "I — natychmiast" itp.). Auto-import z elementów inspekcji nie wypełnia tych pól — inspektor wpisuje ręcznie po ekspertyzie.

**(5) Zakres kontroli — pkt 6/7/8.6 wzorca PIIB (PR #15, commit `07f59c7`).** Audyt: "Zakres kontroli na drugiej stronie deklaruje 7 punktów, ale w protokole brakuje odniesienia do pkt 6 i 7. Brakuje też w ustaleniach końcowych pkt 8.6." Migracja **`2026-05-08_inspection_scope_findings.sql`** dodaje 3 pola TEXT do `inspections`:
- `environmental_protection_findings` — pkt 6: stan techniczny instalacji ochrony środowiska (instalacja odgromowa, oświetlenie nawigacyjne)
- `documentation_verification_findings` — pkt 7: weryfikacja kompletności i aktualności dokumentów (KOB, protokoły serwisowe, certyfikaty UDT)
- `weather_exposure_methods` — pkt 8.6 wzorca: metody i środki użytkowania elementów narażonych na szkodliwe wpływy atmosferyczne (dla turbin zwykle "Nie dotyczy" — turbiny są konstrukcyjnie odporne, brak elementów drewnianych)

UI w `src/app/(protected)/inspekcje/[id]/page.tsx` (+80 linii) — 3 textarea w tabie "Wnioski" edytora inspekcji z sensownymi placeholderami (sugestia tekstu dla turbin: "W trakcie kontroli dokonano przeglądu instalacji i urządzeń służących ochronie środowiska...", "Zweryfikowano kompletność i aktualność dokumentów obiektu: Książka Obiektu Budowlanego (KOB)...", "Nie dotyczy.").

**(6) Karta turbiny — opis techniczny + UDT + Sprzęt ewakuacyjno-ratunkowy (PR #17, commit `38aaac5`).** Audyt: "W protokołach archiwalnych mamy opis techniczny turbiny, w którym znajdziemy jej podstawowe dane. Dobrze by było wyciągnąć te dane do naszej karty turbiny i wykorzystać je w karcie inspekcji." Migracja **`2026-05-08_turbine_technical_description_udt_rescue.sql`** rozszerza w 3 obszarach:

1. **Opis techniczny** — 8 nowych pól w `turbines`: `tower_segments_count INTEGER`, `nacelle_material TEXT`, `blade_material TEXT`, `foundation_diameter_m NUMERIC(6,2)`, `foundation_depth_m NUMERIC(5,2)`, `foundation_concrete_class TEXT`, `pedestal_height_m NUMERIC(4,2)`, `service_crane_capacity_t NUMERIC(5,3)`. Pełnomocne dane fizyczne fundamentu, wieży, materiałów i dźwigu serwisowego.

2. **`turbine_udt_devices` (1:N do turbines)** — urządzenia podlegające pod UDT lub kontrolowane przez uprawniony zespół: `device_type TEXT NOT NULL` ("Podest ruchomy", "Wciągarka serwisowa"), `manufacturer TEXT`, `model TEXT`, `capacity_t NUMERIC(5,3)`, `is_udt_subject BOOLEAN`, `inspection_frequency TEXT`, `certificate_number TEXT`, `last_inspection_date DATE`, `next_inspection_date DATE`, `notes TEXT`, soft-delete `is_deleted`, sortowanie `sort_order`.

3. **`turbine_rescue_equipment` (1:N do turbines)** — sprzęt ewakuacyjno-ratunkowy: `equipment_type TEXT NOT NULL` ("PSA", "Drabina z asekuracją", "Punkty zaczepienia"), `manufacturer`, `model` ("PSA AG 10K"), cykl kontrolny, opis parametrów + uwagi inspektora.

UI:
- **TurbineForm** rozszerzony o sekcję "Opis techniczny — szczegóły" z 11 polami (8 nowych + 3 istniejące: Typ kabla SN / Długość kabla / checkbox "Stacja kontenerowa"). Placeholdery z wartościami referencyjnymi: "5" (segmenty), "kompozyt szklano-węglowy" (materiał), "24.00" (śr. fundamentu), "C30/C37 wg PN-EN 206-1" (klasa betonu), "0.250 (= 250 kg)" (udźwig dźwigu).
- **Karta turbiny** `/turbiny/[id]` (+80 linii) — 2 nowe sekcje "Urządzenia podlegające UDT" i "Sprzęt ewakuacyjno-ratunkowy" z dialog inline edycji (rozwija się w karcie, nie modal), debounce save 800ms, walidacja w UI, soft-delete (Usuń wiersz).
- Nowy komponent `src/components/turbine/udt-rescue-sections.tsx` (+450 linii) — wspólna abstrakcja dla obu sekcji (typed by `equipment_type` enum w wewnętrznym union).

RLS: admin/inspektor pełen dostęp, klient SELECT tylko dla swoich turbin (przez join `turbines` → `wind_farms` → `client_users`).

Renderery PDF/DOCX (PR #13) wyświetlają nową sekcję "Opis techniczny obiektu" w protokole + tabele UDT i Rescue z graceful degradation gdy puste (sekcja pomijana w EW01 bo żaden inspektor jeszcze nie wpisał danych — wymaga osobnej iteracji wypełnienia).

**(7) Workflow commit/PR/merge — kontynuacja z 2026-05-07.** Identyczna ścieżka:
1. `git checkout -b claude/audit-5y-{name}-2026-05-08 origin/main` z worktree `charming-neumann-1a82d5`
2. selektywne `git add <files>` per PR (nie `git add .` żeby zachować pliki dla pozostałych PR-ów)
3. commit z HEREDOC (Co-Authored-By Claude)
4. `git push -u origin claude/{name}` + `gh pr create --base main --head ...`
5. `gh api -X PUT repos/wtrzecki-hub/prowatech-inspekcje/pulls/{N}/merge -f merge_method=squash`
6. `gh api -X DELETE repos/.../git/refs/heads/{branch}` (cleanup)

5 PR-ów (#13, #14, #15, #16, #17) zmergowanych w kolejności A→B→C→D→E. PR-A pierwszy bo zawiera **wszystkie zmiany rendererów** — dzięki temu PR-B/C/D/E to czyste UI+migracje bez konfliktów na `api/pdf/route.ts` / `api/docx/route.ts`. **Lekcja**: gdy wiele uwag dotyka tych samych plików (renderery), lepiej zbundlować je w jeden duży PR niż próbować rozdzielać per-uwaga (cherry-pick z konfliktami).

**(8) Stan końcowy — testy lokalne (przed PR-ami) + verifikacja PDF z EW01.** Wszystkie 7 uwag testowane na localhost (dev server na worktree `charming-neumann-1a82d5`, port 3000) po dodaniu `http://localhost:3000/auth/callback` + `http://localhost:3000/portal/auth/reset` do **Supabase Auth → URL Configuration → Redirect URLs** (Site URL pozostał na produkcji `prowatech-inspekcje.vercel.app` — OAuth używa explicite `redirectTo` z `${origin}/auth/callback`). Bez tego dodania OAuth flow zawracał na produkcję mimo loginu z localhost. Testy wizualne 7 uwag → ✓; PDF z EW01 (001/P/2026, 5 generacji v1→v5) → ✓ wszystkie 6 fix-ów Waldka zaadresowane.

**Komity:** 5 squashed na main: `50707e7` (PR #13 — renderery + 6 fix-ów), `98e16e6` (PR #14 — serwis OFF), `07f59c7` (PR #15 — pkt 6/7/8.6), `6a20a35` (PR #16 — zalecenia urgency), `38aaac5` (PR #17 — turbina opis + UDT + Rescue).

**Pliki nowe:** `migrations/2026-05-08_service_include_default_false.sql`, `migrations/2026-05-08_inspection_scope_findings.sql`, `migrations/2026-05-08_repair_scope_urgency_kind.sql`, `migrations/2026-05-08_turbine_technical_description_udt_rescue.sql`, `src/components/turbine/udt-rescue-sections.tsx`. Wszystkie 4 migracje zaaplikowane przez Supabase MCP `apply_migration` w trakcie sesji 2026-05-08 (przed commitami — kod tylko musiał je dogonić w PR-ach).

**Pliki znacząco zmienione:** `src/app/api/pdf/[id]/route.ts` (+527 linii — 4 nowe sekcje + 6 fix-ów), `src/app/api/docx/[id]/route.ts` (+711 linii — analogiczne sekcje + fix-y D/E/F), `src/components/forms/turbine-form.tsx` (+182 — sekcja "Opis techniczny — szczegóły"), `src/components/inspection/repair-scope-table.tsx` (+113 — kolumny urgency/work_kind/element), `src/app/(protected)/inspekcje/[id]/page.tsx` (+80 — 3 textarea pkt 6/7/8.6), `src/app/(protected)/turbiny/[id]/page.tsx` (+80 — sekcje UDT/Rescue na karcie), `src/components/forms/turbine-inspection-form.tsx` (+1/-1 — useState OFF), `src/lib/design/protocol-tokens.ts` (+30 — ART5_MET_COLORS).

**Otwarte tematy (nieblokujące):**
- Auto-fill `documents_reviewed.previous_5y/annual.info` z `historical_protocols` (scenariusz 1 z workflow Waldka — turbina obsługiwana przez nas). Aktualnie inspektor wpisuje ręcznie nawet gdy w archiwum są dane.
- Wpisanie wartości UDT/Rescue dla EW01 (i innych turbin obsługiwanych) — dane są dostępne z protokołów archiwalnych, ale wymaga ręcznej iteracji w UI lub batch import z PDF.
- Klonowanie UDT/Rescue do snapshot inspekcji (live vs frozen): aktualnie sekcja w PDF/DOCX bierze aktualne wartości z `turbine_udt_devices` — co znaczy że stary, signed protokół po edycji UDT pokaże nowe dane przy kolejnym renderze. Pattern PIIB z 2026-05-07 (snapshot pól PIIB do `inspection_metadata`) można rozszerzyć.
- Sekcja "Inne zalecenia (bez przypisanego źródła)" — `previous_recommendations` z `source_inspection_type=NULL` (legacy, ~10 wpisów). Pattern UX: hurtowe migracje + przyciski w nagłówku ("Migruj wszystko do 5-letniej").

**Lekcje sesji (do PROGRESS.md gotchas):**
1. **Multiple uwag w jednym pliku → bundle do jednego PR** — gdy zmiany dotyczą tych samych plików (renderery), próba rozdzielenia per-uwaga generuje konflikty 1:1 line. Lepiej zbundlować i merge'ować pierwsze.
2. **Supabase Auth redirect na lokal** — Site URL może wskazywać na produkcję, ale `Additional Redirect URLs` musi zawierać `http://localhost:3000/auth/callback` (explicite ścieżka, bez `**` wildcard). Bez tego OAuth z localhost zawraca na produkcję bo Supabase odrzuca redirect_uri jako nieautoryzowane.
3. **PDF iteracje testowe** — `start "" "<plik>.pdf"` w Bash uruchamia default viewer (Acrobat), ale po otwarciu plik jest locked i `rm` failuje "Device or resource busy". Można skopiować z `Downloads` do worktree dla `pdftotext -layout` extraction (Git Bash MinGW ma `pdftotext.exe`), bez ruszania oryginału.
4. **Heurystyka pierwszej kontroli** — `!sourceInfo && rows.length === 0` to wystarczająca detekcja "pierwszej kontroli {typu}" bez nowej flagi w schemacie. Działa dla 3 scenariuszy workflow Waldka (kontynuacja / nowy obiekt / pierwsza).
5. **`addSubHeading` globalny fix** — splitTextToSize w helperze rozwiązuje problem dla wszystkich call sites, nie tylko dla aktualnie problematycznego tytułu. PR #11 z 05-07 (split bold+italic body w sekcji zdjęć) jest teraz redundantny — można uprościć w przyszłej iteracji.
6. **`gh pr merge` w worktree fail** (z 05-07): kontynuacja workaround `gh api -X PUT repos/.../pulls/{N}/merge -f merge_method=squash`. Działa stabilnie.

---

### 2026-05-07 — Audyt UI inspekcji 5-letniej + sekcja zdjęć w protokole + auto-renumeracja zaleceń

**Sesja długa, 10 PR-ów zmergowanych do main, 3 migracje Supabase, ~1500 linii zmian.** Praca na worktree `claude/gifted-aryabhata-7b0445` w trybie commit→PR→merge przez `gh` CLI (zalogowany w trakcie sesji przez `gh auth login --web`). Vercel auto-deploy z `main`. Punktem startowym był audyt Waldka na inspekcji EW01 — FW Żeńsko (5-letnia, protokół 001/P/2026, status `completed`, 15 zdjęć, 11 zaleceń poprzednich + 10 zakresu robót).

**(1) Metryczka inspekcji — strony protokołu z bazy klienta (PR #2, commit `5cdc82a`).** Trzy free-text pola w sekcji "Strony protokołu" zmienione na multi/single-select z bazy:
- **Wykonawca kontroli** (`contractor_info` legacy → multi-select z `inspectors`): checkboxy ze wszystkimi aktywnymi inspektorami, walidacja w UI (ostrzeżenie, nie blokada): min. 2 osoby (bezpieczeństwo), dla 5-letniej wymagana konstrukcyjna + elektryczna. Zapis do istniejącej `inspection_inspectors` junction (z `is_lead` + `specialty`). Stary `contractor_info` zachowany jako legacy, pokazany z X (clear) gdy wypełniony. Renderery PDF/DOCX bez zmian — już używały fallbacku do `inspection_inspectors` gdy `contractor_info` puste.
- **Przy udziale** (`additional_participants` legacy → multi-select przedstawicieli klienta): nowa tabela `client_representatives` (per klient, z RLS) + junction `inspection_participants`. Inline dialog "Dodaj przedstawiciela" (imię + rola + tel + email) zapisuje do bazy klienta i auto-zaznacza dla bieżącej kontroli. Hover na wpisie → ikona kosza dla soft-delete (`is_deleted=true`). Generator PDF/DOCX przepisany żeby joinować `inspection_participants → client_representatives` z fallbackiem do legacy `additional_participants`.
- **Zarządca obiektu** (`manager_name` TEXT zostaje, ale zasilane z bazy): Select dropdown z `client_representatives` + przycisk `+` obok otwiera **ten sam dialog** dodawania, ale z kontekstem `'manager'` zamiast `'participant'`. Po dodaniu nowy rep ląduje w `manager_name` jako sformatowany tekst "Imię Nazwisko (rola)". Pole tekstowe zostawione jako fallback dla własnego wpisu — gdy `manager_name` nie pasuje do żadnego rep-a, w dropdownie pojawia się dodatkowa opcja `__custom__` "X (własny wpis)" + komunikat "Stary wpis (legacy)" z X (clear). Pattern zaadaptowany do `electrical_measurement_final_assessment` w późniejszych PR-ach.

**Migracja `2026-05-07_client_representatives_and_participants.sql`** (zaaplikowana via Supabase MCP):
```sql
CREATE TABLE client_representatives (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  client_id UUID NOT NULL REFERENCES clients(id) ON DELETE CASCADE,
  full_name TEXT NOT NULL, role TEXT, phone TEXT, email TEXT, notes TEXT,
  is_active BOOLEAN NOT NULL DEFAULT TRUE, is_deleted BOOLEAN NOT NULL DEFAULT FALSE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE TABLE inspection_participants (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  inspection_id UUID NOT NULL REFERENCES inspections(id) ON DELETE CASCADE,
  representative_id UUID NOT NULL REFERENCES client_representatives(id) ON DELETE RESTRICT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE (inspection_id, representative_id)
);
```
RLS: admin/inspector pełen dostęp, klienci widzą tylko swoich (przez `client_users` join).

**(2) Pola PIIB turbiny — edytowalne w 3 miejscach (PR #2 cd.).** Cztery pola z `2026-04-25_piib_turbine_fields.sql` (rodzaj konstrukcji wieży `tower_construction_type` enum stalowa/żelbetowa/hybrydowa/inna, rok zakończenia budowy `commissioning_year` int 1980-2099, nr i data pozwolenia na budowę `building_permit_number/_date`) były dotąd renderowane w PDF/DOCX ale nigdzie nie edytowalne — w bazie wszystkie NULL. Dodane w 3 miejscach:
- `TurbineForm` (dialog edycji turbiny) — `<select>` dla typu wieży, number input dla roku, text+date inputs dla pozwolenia, w grid 2-kolumnowym.
- Karta turbiny (`/turbiny/[id]`) — 3 nowe `InfoItem` w sekcji "Dane techniczne" + przekazanie pól do `initialData` w dialogu edycji.
- Metryczka inspekcji (`InspectionMetadataPiib`) — nowa karta "Dane techniczne obiektu" pomiędzy "Metryczka obiektu" a "Strony protokołu". Edycja zapisuje **bezpośrednio do `turbines`** (debounce 800ms, link "Otwórz kartę turbiny" + komunikat "Zmiany zobaczysz przy kolejnych inspekcjach"). To rozwiązuje problem retroaktywnego uzupełniania pól dla starych protokołów: w połączeniu z **klikalnymi kółkami StatusBar (forward + backward)** + dialog cofnięcia z zachowaniem dat podpisów, inspektor może cofnąć podpisany protokół do `draft`, uzupełnić brakujące pola (rodzaj wieży itp.), ponownie przejść do `signed`. Auto-trigger `completed → signed` z migracji 2026-04-26 nie koliduje — odpala się tylko `BEFORE UPDATE OF` dat podpisu lub statusu.

**(3) Zalecenia z poprzednich kontroli — 2 sekcje (5-letnia + roczna) (PR #2 cd.).** Audyt: w protokole PIIB sekcja II "Sprawdzenie wykonania zaleceń" wymaga sprawdzenia **z OBU** poprzednich kontroli (nie tylko ostatniej). Dotąd auto-import w `previous-recommendations-table.tsx` ciągnął tylko `LIMIT 1` z poprzedniej zakończonej inspekcji niezależnie od typu — przez co dla turbiny z ostatnią 5-letnią w bazie/archiwum brakowało zaleceń z rocznej. **Migracja `2026-05-07_previous_recs_source_type.sql`**: dodaje `previous_recommendations.source_inspection_type` TEXT (`annual` | `five_year` | NULL) + indeks `(inspection_id, source_inspection_type)`. **Komponent przepisany** na 2 osobne `<Card>` sekcje (5-letnia + roczna), każda z własnym auto-importem (na pierwszym mount, niezależnie dla każdego typu) i bannerem. Warstwy fallbacku per typ rozszerzone o `historical_protocols` (PDF-only z archiwum: pokazuje header z datą + nr protokołu + linkiem "otwórz PDF z archiwum", strukturalnych zaleceń nie wyciąga — inspektor wpisuje ręcznie z PDF). Itemy bez `source_inspection_type` (legacy z migracji) lądują w 3-ciej karcie "Inne zalecenia (bez przypisanego źródła)" — backward-compat. Subkomponenty `SourceSection` + `RecommendationList` wyodrębnione żeby uniknąć duplikacji renderu.

**(4) Strukturalne oględziny w pomiarach (PR #2 cd. + PR #3, #5, #6).** W zakładce Pomiary (5-letnia) po orzeczeniu/ocenie końcowej dodane 2 strukturalne pozycje "Oględziny instalacji elektrycznej" i "Oględziny instalacji odgromowej i uziomów" — każda z wyborem `pozytywna`/`negatywna` + warunkowy opis przy negatywnej (subkomponent `VisualInspectionItem`, czyszczenie notes auto przy zmianie z negatywnej na pozytywną/null żeby nie zostawić osieroconych danych). **Migracja `2026-05-07_visual_inspection_results.sql`** dodaje 4 kolumny TEXT z CHECK constraint na enum: `electrical_visual_inspection_result/_notes` + `lightning_visual_inspection_result/_notes`. PDF/DOCX renderery dorzucają 2 wiersze w summary table z formatem "Pozytywna" lub "Negatywna — {opis}". `hasSummary` uwzględnia nowe pola. **Iteracje po feedbacku Waldka** (3 PR-y w jednym wątku):
- **PR #3 (`32229be`)** — reorder: oględziny POPRZEDZAJĄ Ocenę końcową (jest podsumowaniem oględzin + orzeczenia, więc na końcu logicznie). Czysta zmiana kolejności JSX.
- **PR #5 (`42f3ba7`)** — Ocena końcowa: textarea → Select (Pozytywna/Negatywna). Uwagi do oględzin i oceny widoczne tylko gdy ocena = Negatywna. Bez migracji bazy — używamy `electrical_measurement_final_assessment` TEXT z lowercase wartościami, legacy free-text obsłużone jako `__custom__` (analog do Zarządca z PR #2). PDF/DOCX dostają capitalize (`pozytywna` → `Pozytywna`).
- **PR #6 (`a6d402c`)** — cleanup po feedbacku "trochę bałagan": Waldek miał ten sam tekst opisu w 2 polach (`lightning_visual_inspection_notes` + `electrical_measurement_notes` — bo wpisał najpierw przy oględzinach, potem przy uwagach które były zawsze widoczne). Wybór: zostawiamy TYLKO opisy per-Oględziny, top-level "Uwagi do oględzin i oceny" znika z UI. Legacy `electrical_measurement_notes` pokazujemy w UI jako mały szary banner "Stary tekst (legacy)" z X (clear) — żeby user mógł przekleić gdzie pasuje albo wyczyścić. PDF/DOCX dalej renderują legacy wartość (backward compat dla starych protokołów z archiwum).

**(5) Auto-renumeracja zaleceń per-sekcja + edytowalny numer pozycji (PR #7 `aa2da0b` + PR #10 `9305960`).** Po cleanupie zaleceń w EW01 Waldek odkrył: po usuwaniu/dodawaniu pozycji numeracja zostawała z dziurami (np. 9, 10, 11, 15, 16) bo `nextNumber = max + 1` globalnie i brak renumerow po delete. **Pierwsze podejście (PR #7) dla `previous_recommendations`**: helper `renumberSection(sb, forType)` (SELECT po item_number ASC, UPDATE na sekwencyjne 1..N, sync state lokalnego), wywoływany po `handleAdd`/`handleDelete`/`runAutoImport` per-grupa. `handleNumberChange(id, raw)` — manual override (debounce 600ms, save raw value, bez auto-resort, user decyduje). Static kółko z numerem (font-mono, bg-graphite-100) zamienione na `<input type=number>` styled jak kółko (spin buttons ukryte tailwindem). Sortowanie `sortedItems = items.sort(item_number ASC)` przed grupowaniem — manual edit numerka przesuwa wpis natychmiast w UI. **Drugie podejście (PR #10)** rozszerza ten sam pattern na trzecią sekcję `repair_scope_items` ("VI. ZALECENIA — ZAKRES ROBÓT REMONTOWYCH"): `renumberAll(sb)` (bez source_inspection_type, jedna numeracja per inspekcja), `handleNumberChange`, editable input. Po obu PR-ach: **3 niezależne liczniki 1..N** — five_year previous, annual previous, repair_scope_items. Każda sekcja zaczyna od 1, zamyka dziurę po delete, manual override przez kliknięcie numeru.

**(6) Sekcja "Dokumentacja fotograficzna" w PDF i DOCX (PR #8 `98be0f7` + PR #11 `90a68ed`).** Audyt: w generowanych protokołach brakowało siatki zdjęć z `inspection_photos` ("Zdjęcie nr 1, 2, 3, ..." — jak w archiwalnych protokołach). UI miał upload (PhotoGallery → 15 zdjęć dla EW01), ale renderer PDF/DOCX wyświetlał tylko placeholder text "Numerację fotografii zsynchronizowano z kolumną 'Nr fot.' w tabeli ustaleń (sekcja III)". **Implementacja w obu generatorach**: fetch `inspection_photos` z `file_url` posortowane po `photo_number` ASC, pre-fetch wszystkich obrazów równolegle (`Promise.all` + `fetchImageAsBase64` / `fetchImageAsBuffer`), siatka **2 zdjęcia w wierszu** z captionami "Zdjęcie nr X" italic poniżej. Format 3:2 aspect (PDF: ~85mm szerokości, DOCX: 280×187 px). Graceful degradation: placeholder ramka/tekst gdy fetch nie pobrał (404, sieć itp.). WEBP → JPEG fallback dla jsPDF. **Bug "deploy lag"** — Waldek po pierwszym deploy zgłosił "są numery zdjęć w sekcji elementy ale nie ma samych zdjęć" — sprawdzenie via Supabase MCP `execute_sql` potwierdzilo 15 wierszy z file_url, R2 URL accessible (curl 200, image/jpeg, 315 KB), RLS SELECT=true. **Hipoteza: deploy timing + browser cache** — zalecony Ctrl+Shift+R + Vercel logs check. Dodane logi diagnostyczne (PR #9): `[PDF] inspection_photos dla {id}: N wierszy` + `[PDF] photoRows z file_url: M`. Po deploy zdjęcia załadowały się prawidłowo. **PR #11** — fix overflow tytułu: "Dokumentacja fotograficzna wykonana podczas kontroli (elementy obiektu, posiadające usterki lub wady, przewidziane do remontu)" wychodził poza margines A4 w PDF (jsPDF `subHeading` nie zawija). Rozdzielenie na bold subHeading "Dokumentacja fotograficzna wykonana podczas kontroli" + italic body "(elementy obiektu posiadające usterki lub wady, przewidziane do remontu)".

**(7) Workflow commit/PR/merge przez `gh` CLI.** W połowie sesji Waldek zalogował się do `gh` przez `gh auth login --web` z Git Bash (Windows-side, host) — token zapisał się w `C:\Users\WALDEK\.config\gh\hosts.yml` i sandbox sesja od razu zobaczył auth (`gh auth status` → `Logged in as wtrzecki-hub`). Od PR #2 wszystkie zmiany lądowały na main przez:
1. `git checkout -b claude/{name} origin/main` (świeży branch z aktualnego main)
2. edycja → commit z HEREDOC body (Co-Authored-By Claude)
3. `git push -u origin claude/{name}`
4. `gh pr create --base main --head ...`
5. `gh api -X PUT repos/.../pulls/{N}/merge -f merge_method=squash` (skrót — `gh pr merge` failuje na "fatal: 'main' is already used by worktree" bo main jest checkoutowany w katalogu parent)
6. `gh api -X DELETE repos/.../git/refs/heads/claude/{name}` (cleanup brancha)

**Gotcha sandbox-vs-Edit**: kilka razy `git checkout claude/{branch} -- file` z worktree powodował utratę uncommitted changes (np. cherry-pick PR #5 stracił edit do `previous-recommendations-table.tsx` przy resecie do main, musiałem odtworzyć ~5 edycji ręcznie). Lekcja: **zawsze commit lokalny przed switchem brancha**, nie polegać na `git checkout -- file` do migracji uncommitted changes między branchami.

**(8) Stan końcowy danych w bazie po sesji** (verified SQL przez Supabase MCP):
- `inspection_photos`: 15 wierszy dla EW01 (000/P/2026), wszystkie z `file_url` na R2 (~300 KB każde)
- `previous_recommendations` dla EW01:
  - `five_year`: 8 wierszy numerowanych 1-8 ✓
  - `annual`: 3 wiersze numerowane 9-11 (pre-fix data — sprzed PR #7 renumber)
  - dla aktywnego draftu 73f54344: five_year 1-10 (po fix-ie) ✓, ungrouped 10 (legacy)
- `repair_scope_items`: 10 dla 001/P/2026, 3 dla draftu 73f54344 (Waldek miał na chwilę impresję że "było 10 jest obecnie 3" — to inspekcja-draft a nie completed, dane się nie zgubiły)

**Komity:** 9 squashed na main: `5cdc82a` (PR #2 — duży rewrite metryczki), `32229be` (PR #3 — reorder pomiary), `42f3ba7` (PR #5 — Ocena Select), `a6d402c` (PR #6 — cleanup Uwagi), `aa2da0b` (PR #7 — renumber previous_recs), `98be0f7` (PR #8 — photos w PDF/DOCX), `681c530` (PR #9 — debug logs), `9305960` (PR #10 — renumber repair_scope), `90a68ed` (PR #11 — title wrap). PR #4 zamknięty (force-push utracił link), zastąpiony przez #5.

**Pliki nowe:** `migrations/2026-05-07_client_representatives_and_participants.sql`, `migrations/2026-05-07_previous_recs_source_type.sql`, `migrations/2026-05-07_visual_inspection_results.sql`. Wszystkie 3 zaaplikowane przez Supabase MCP `apply_migration` w trakcie sesji.

**Pliki znacząco zmienione:** `src/components/inspection/inspection-metadata-piib.tsx` (+~900 linii, multi-select inspektorów + przedstawicieli, dialog dodawania per kontekst, sekcja PIIB turbiny), `src/components/inspection/electrical-measurements.tsx` (oględziny + Ocena końcowa Select + reorder), `src/components/inspection/previous-recommendations-table.tsx` (rewrite na 2 sekcje + renumber + editable number), `src/components/inspection/repair-scope-table.tsx` (renumber + editable number), `src/components/inspection/status-bar.tsx` (klikalne kółka + cofanie do szkicu), `src/components/forms/turbine-form.tsx` (4 nowe pola PIIB), `src/app/(protected)/turbiny/[id]/page.tsx` (3 nowe InfoItem + dialog passthrough), `src/app/api/pdf/[id]/route.ts` + `src/app/api/docx/[id]/route.ts` (sekcja zdjęć + renderowanie nowych pól metryczki + oględziny + capitalize Ocena).

**Otwarte tematy (nieblokujące):**
- "Inne zalecenia (bez przypisanego źródła)" w `previous_recommendations` — legacy NULL source items, dla draftu 73f54344 jest tam 10 wpisów które user widzi obok 5-letniej z tymi samymi treściami. Pattern UX: można dodać przycisk "Migruj wszystko do 5-letniej" + "Migruj wszystko do rocznej" + "Usuń wszystkie" w nagłówku tej sekcji. Decyzja na potem.
- WindFarmForm (`/farmy/[id]`) nie ma input dla `area_label` (pole z 2026-04-28). Można dorzucić select "Wschód/Zachód/Południe/Inny/[brak]".
- Hero `/farmy/[id]` można dorobić chip `Obszar: Wschód` pod tytułem farmy.
- Generator PDF `addSubHeading` nie zawija — przy długich tytułach overflow. Można rozszerzyć helper o `splitTextToSize` automatycznie. Workaround tymczasowy w PR #11 przez split na bold+italic body.

**Lekcje sesji (do PROGRESS.md gotchas):**
1. **`gh pr merge` fail w worktree** — `gh pr merge` próbuje checkout brancha lokalnie, fail na "fatal: 'main' is already used by worktree at 'C:/prowatech-inspekcje'". Workaround: `gh api -X PUT repos/{owner}/{repo}/pulls/{N}/merge -f merge_method=squash` + `gh api -X DELETE repos/{owner}/{repo}/git/refs/heads/{branch}`.
2. **`gh api` z leading slash** — `/repos/...` powoduje `invalid API endpoint: "C:/Program Files/Git/repos/..."` (Git Bash on Windows interpretuje `/` jako path prefix). Zawsze bez slash: `repos/...`.
3. **`git checkout {branch} -- file` traci uncommitted edits** — uncommitted changes na bieżącym branchu giną gdy `checkout` z innego brancha pobiera plik. Komituj lokalnie zanim przełączasz.
4. **`addSubHeading` w jsPDF nie zawija** — long titles overflow. Workaround: split na bold + italic body, albo użyj `splitTextToSize` przed `pdf.text`.
5. **Vercel deploy timing** — po `gh api PUT merge`, deploy zaczyna się ~30s później, build ~1-2 min. User może odświeżać i nie widzieć zmian — sprawdzić `gh api repos/.../deployments?per_page=1` (state: success) zanim się odpisze.
6. **Cherry-pick conflict gdy main ma squashed merge** — moje commity z brancha A miały content już zawarty w squashed commit X na main. Cherry-pick mojego nowego commitu Y na fresh branch z main → konflikt bo rebase wymaga 1:1 line context. Rozwiązanie: `git reset --hard origin/main` + `git cherry-pick {nowy-commit}` (bez wcześniejszych commitów z A).
